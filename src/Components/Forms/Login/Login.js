import React from 'react';
import Form from './LoginForm';

const Login = () => {
  return (
    <Form
      FormTitle="Login"
      formMessage="Don't have an account yet? SignUp Here."
    />
  );
};

export default Login;
